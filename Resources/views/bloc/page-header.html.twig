<div class="page-header">
	<h1>{{ entiteName|capitalize }}
		{% if ssentiteName is defined %}
		<small> | {{ ssentiteName|upper }}</small>
		{% endif %}
		{% if action2 == "annule" %}
		<a href="{{ url(ROUTE, {action: 'liste', classEntite: Url_encode(URLclassEntite)}) }}">
			<button type="button" class="btn btn-primary btn-md"><span class="glyphicon glyphicon-arrow-left"></span> Retour</button>
		</a>
		{% endif %}
	</h1>
	{% if comment is defined %}<p>{{ comment|capitalize }}</p>{% endif %}
</div>